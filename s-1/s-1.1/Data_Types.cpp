#include<bits/stdc++.h>
using namespace std;

int32_t main()
{
    // data types:- 
    // int [-10pow(9) to 10pow(9)], long[-10pow(12) to 10pow(12)], long long[-10pow(18) to 10pow(18)], float, double, long double
    // string, getline
    // char
    
    // int x, y;
    // long x, y;
    // long long x, y;
    // float x, y;
    // double x, y;
    // string x, y;
 
    // string str;
    // getline(cin, str);
    // cout << "value of str: " << str; 
    
    char x, y; // https://www.cs.uah.edu/~rcoleman/Common/Basics/ASCIICharSet.html#:~:text=A%20char%20variable%20in%20C%2B%2B,in%20the%20ASCII%20character%20set.
               // ASCII value of A - 65 && a - 97 && 0 - 48
    cin >> x >> y;
    cout <<"value of x: "<< x << " and value of y: " << y;
}